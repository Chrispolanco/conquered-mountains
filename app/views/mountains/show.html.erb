<h1> <%= @mountain.name %> </h1> 

<h3> Has Been Conquered <%= pluralize(@mountain.explorations, 'Time')%> </h3> 

<h3> Climbers who have completed Climb </h3> 
    <% @mountain.climbs.each do |climb| %> 
        <% uniq_climbers = []%>
        <% uniq_climbers << climb.climber%>
        <% uniq_climbers.uniq.each do |unique_climber| %>

            <div> <%= link_to unique_climber.name, climber_path(unique_climber)%> 
        </div>
        <% end %>

    <% end %> 
<br> 
<br> 
<%= link_to 'Edit', edit_mountain_path(@mountain) %>
