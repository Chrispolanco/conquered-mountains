<%= stylesheet_link_tag "mountains" %>

<%= form_for @mountain do |f| %> 

<%= render partial: 'layouts/errors', locals: {object: @mountain}%>

    <div class="field">
        <%= f.label 'Name' %><br>
        <%= f.text_field :name %><br> 
    </div> 

    <div class="actions">
        <%= f.submit "Submit Mountain" %>
    </div> 

<% end %> 
