<%= form_for @climb do |f| %> 

<%= render partial: 'layouts/errors', locals: {object: @climb}%>

    <div class="field">
        <%= f.label 'Date Started' %><br>
        <%= f.date_select :climb_start %><br> 
    </div> 

    <div class="field">
        <%= f.label 'Date Finished' %><br>
        <%= f.date_select :date_completed %><br> 
    </div> 

    <div class="field"> 
        <%= f.label :mountain %>
        <%= f.collection_select :mountain_id, Mountain.all, :id, :name %>
    <div class="field">

    <div class="field"> 
        <%= f.label :climber %>
        <%= f.collection_select :climber_id, Climber.all, :id, :name %>
    <div class="field">

    <div class="actions">
        <%= f.submit "Submit Climb" %>
    </div>         

<% end %> 


